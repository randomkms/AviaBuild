@model IEnumerable<AviaBuildWeb.Models.Area>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Ceh.IdCeh)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Ceh.IdCeh)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdArea }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdArea }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdArea })
        </td>
    </tr>
}

</table>
