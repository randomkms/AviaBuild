@model IEnumerable<AviaBuildWeb.Models.EngTehProf>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.EngTehProfName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.EngTehProfName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdEngTehProf }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdEngTehProf }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdEngTehProf })
        </td>
    </tr>
}

</table>
