@model AviaBuildWeb.Models.Brigade

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>Brigade</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Area.IdArea)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Area.IdArea)
        </dd>

    </dl>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.IdBrigade }) |
    @Html.ActionLink("Back to List", "Index")
</p>
