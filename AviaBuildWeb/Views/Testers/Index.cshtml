@model IEnumerable<AviaBuildWeb.Models.Tester>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TesterName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TestLab.TestLabName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TesterName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TestLab.TestLabName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdTester }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdTester }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdTester })
        </td>
    </tr>
}

</table>
