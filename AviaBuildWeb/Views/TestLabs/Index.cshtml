@model IEnumerable<AviaBuildWeb.Models.TestLab>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TestLabName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TestLabName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdTestLab }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdTestLab }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdTestLab })
        </td>
    </tr>
}

</table>
