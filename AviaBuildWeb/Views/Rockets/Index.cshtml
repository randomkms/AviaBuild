@model IEnumerable<AviaBuildWeb.Models.Rocket>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.RocketName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RocketType)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Product.Category)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.RocketName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RocketType)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Product.Category)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdRocket }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdRocket }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdRocket })
        </td>
    </tr>
}

</table>
