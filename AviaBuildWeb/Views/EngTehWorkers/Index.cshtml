@model IEnumerable<AviaBuildWeb.Models.EngTehWorker>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.EngTehWorkerName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IsHead)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IsMaster)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UnderHeadId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Area.IdArea)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.EngTehWorkerName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IsHead)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IsMaster)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.UnderHeadId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Area.IdArea)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdEngTehWorker }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdEngTehWorker }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdEngTehWorker })
        </td>
    </tr>
}

</table>
