@model IEnumerable<AviaBuildWeb.Models.Plane>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PlaneName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PlaneType)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Product.Category)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PlaneName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PlaneType)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Product.Category)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdPlane }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdPlane }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdPlane })
        </td>
    </tr>
}

</table>
